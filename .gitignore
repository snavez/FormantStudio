__pycache__/
.pytest_cache/
*.pyc
*.wav
*.formants
*.TextGrid
*.textgrid

# PyInstaller
build/
dist/
# PyInstaller writes throwaway specs; ours is hand-maintained and carries the
# bundled aligner model, so a build from a fresh clone depends on it.
*.spec
!FormantStudio.spec
FormantStudio.exe

# Misc
nul
.claude/
formant_editor_SpectroCanvas.py
tmp/
